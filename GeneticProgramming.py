import random
import numpy
import Tree
import copy

def crossover (parent1, parent2):
    copy1 = copy.deepcopy(parent1)
    copy2 = copy.deepcopy(parent2)


def selectSub(tree):
    count = tree.countNodes()

def isType(nodeType, tree1):
    if (not tree1):
        return False
    elif (type(tree1.value) is not type(nodeType)):
        return False
    else:
        if (tree1.value == "x" and tree2.value != "x"):
            return False
        elif (tree1.value != "x" and tree2.value == "x"):
            return False
        else:
            return True


def pickNode(tree, randNum):
    stack = []
    left = None
    right = None

    stack.append(tree)
    count = 0
    if (isType())



    if 

